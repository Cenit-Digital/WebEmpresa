{
  "name": "ponytail-vendored-hooks",
  "private": true,
  "type": "commonjs",
  "description": "Scopes the vendored ponytail hook scripts back to CommonJS. The repo root package.json sets \"type\": \"module\", which would otherwise make these require()-based hooks fail as ESM. Node uses the nearest package.json, so this file keeps .claude/ponytail/**/*.js running as CommonJS with zero edits to the upstream hook logic."
}
