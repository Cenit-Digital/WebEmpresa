# tools/

La prueba de mutación se hace con **StrykerJS** (`pnpm mutation`,
config en `stryker.config.json`), no con un mutador propio. Esta carpeta
queda reservada para scripts auxiliares del repo (generadores, comprobaciones
ad-hoc, etc.).
